import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_anmi/app/modules/producto/producto_controller.dart';
import 'package:app_anmi/app/modules/producto/producto_module.dart';

void main() {
  initModule(ProductoModule());
  // ProductoController producto;
  //
  setUp(() {
    //     producto = ProductoModule.to.get<ProductoController>();
  });

  group('ProductoController Test', () {
    //   test("First Test", () {
    //     expect(producto, isInstanceOf<ProductoController>());
    //   });

    //   test("Set Value", () {
    //     expect(producto.value, equals(0));
    //     producto.increment();
    //     expect(producto.value, equals(1));
    //   });
  });
}

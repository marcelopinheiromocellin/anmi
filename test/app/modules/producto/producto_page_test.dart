import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_anmi/app/modules/producto/producto_page.dart';

main() {
  testWidgets('ProductoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProductoPage(title: 'Producto')));
    //  final titleFinder = find.text('Producto');
    //  expect(titleFinder, findsOneWidget);
  });
}
